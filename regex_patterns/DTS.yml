name: DTS
pattern: \bDTS(\b|\d)
description: >-
  **Core DTS (« DTS » générique hors extensions HD ou X**) juste suivie canalisation numérique (ex **`DTS5`**, **`DTS.5`**) — format audio compressé ancien télé/broadcast léger remaster Blu-ray française.
tags:
- Audio
tests:
- id: 1
  input: Film.DTS.5.1.720p.BluRay.x264-GROUP
  expected: true
- id: 2
  input: Film.AC3.2.0.720p.BluRay.x264-GROUP
  expected: false
