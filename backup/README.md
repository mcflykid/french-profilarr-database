# backup/

Archives et modèles pour **Profilarr v2** (PCD schema 1.1.0).

| Dossier | Contenu |
|---------|---------|
| **`snapshot-main/`** | Copie figée de la base (`ops/`, `pcd.json`, `docs/`, `README.md`). |
| **`pcd-vierge/`** | Squelette PCD vide (même `ops/`, sans données). |
| **`docs/compose-profilarr-v2.yml`** | Docker Compose Profilarr + parser (homelab). |

La base active pour Profilarr reste à la **racine** du dépôt (`pcd.json`, `ops/`).
