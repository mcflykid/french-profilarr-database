# PCD vierge — Profilarr v2

Squelette **sans données** (schema [1.1.0](https://github.com/Dictionarry-Hub/schema)).

1. Éditer `pcd.json`.
2. Décommenter et remplir les `INSERT` dans `ops/` (ordre 01 → 12).
3. Nouveau dépôt Git à la racine de ce dossier, ou copier `ops/` + `pcd.json` dans ton projet.

Référence complète : `../snapshot-main/`.
